# ds-calc
 Wordpress plugin that calculates distance between database posts and given user location.

Input uses google maps address autocomplete to find an address, finds distance between the given address and all ACF field address values.
Usage (Shortcode) : [store_distance categories='category1,category2']